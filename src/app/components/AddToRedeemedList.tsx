'use client'
import React from 'react'

const AddToRedeemedList = () => {
  return (
    <div>
        <button onClick={()=>console.log('Click')}>Redeem</button>
    </div>
  )
}

export default AddToRedeemedList